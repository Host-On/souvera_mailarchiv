/**
 * Souvera Archive — Admin Settings (minimal, verweist auf Central)
 */
document.addEventListener('DOMContentLoaded', function () {
	console.log('Souvera Archive admin settings — config in Central')
})
